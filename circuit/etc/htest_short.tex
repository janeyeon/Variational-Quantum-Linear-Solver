( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{{d} - {e}}{2} )}} - {{\cos( \frac{a}{2} )}{\cos( \frac{b}{2} )}{\sin( \frac{{d} + {e}}{2} )}}} )} + {{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b}{2} )}{\cos( \frac{{d} + {e}}{2} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b}{2} )}}} ){\sin( \frac{f}{2} )}}} )} - {{\sin( \frac{c}{2} )}( {{{\cos( \frac{b}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{{d} - {e}}{2} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{{d} + {e}}{2} )}} - {{\cos( \frac{{d} + {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{f}{2} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} - {{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}} + {{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{{d} - {e}}{2} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{f}{2} )}} - {{\cos( \frac{f}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}{\sin( \frac{{d} + {e}}{2} )}}} )}} )} + {{\sin( \frac{b}{2} )}( {{( {{{\cos( \frac{{d} - {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} + {1}} )} )}{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}} - {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\cos( \frac{{d} + {e}}{2} )}{\cos( {\frac{1}{2}( {{g} + {h} - {1}} )} )}}} ){\sin( \frac{f}{2} )}} + {{\cos( \frac{f}{2} )}( {{{\cos( \frac{{g} + {h}}{2} )}{\cos( \frac{1}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{{d} - {e}}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\sin( \frac{{d} + {e}}{2} )}}} )} + {( {{{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{{d} - {e}}{2} )}} - {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\sin( \frac{{d} + {e}}{2} )}}} ){\sin( \frac{{g} + {h}}{2} )}{\sin( \frac{1}{2} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{f}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{b}{2} )}{\sin( \frac{{d} + {e}}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{{d} - {e}}{2} )}}} ){\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {( {{{\cos( \frac{a}{2} )}{\cos( \frac{b}{2} )}{\cos( \frac{{d} + {e}}{2} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b}{2} )}}} ){\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} + {{\sin( \frac{c}{2} )}( {{{\cos( \frac{b}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} - {{\cos( \frac{f}{2} )}{\sin( \frac{{d} - {e}}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} - {{\cos( \frac{a}{2} )}{\sin( \frac{b}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( \frac{f}{2} )}{\sin( \frac{{d} + {e}}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )}} )^{2} + ( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{f}{2} )}( {{{\cos( \frac{1}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\sin( \frac{{d} + {e}}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{{d} - {e}}{2} )}}} ){\sin( \frac{{g} + {h}}{2} )}} + {{\cos( \frac{{g} + {h}}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}{\sin( \frac{{d} - {e}}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\sin( \frac{{d} + {e}}{2} )}}} ){\sin( \frac{1}{2} )}}} )} + {{\sin( \frac{f}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}{\cos( \frac{{d} + {e}}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{{d} - {e}}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}} - {{\cos( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}}} )} + {{\cos( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\cos( \frac{f}{2} )}{\sin( \frac{{d} + {e}}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{a}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\cos( \frac{f}{2} )}( {{{\cos( \frac{b}{2} )}{\cos( \frac{c}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}} - {{\sin( \frac{b}{2} )}{\sin( \frac{c}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}}} )} + {{\sin( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}( {{{\sin( \frac{b}{2} )}{\sin( \frac{c}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}} - {{\cos( \frac{b}{2} )}{\cos( \frac{c}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}}} )}} )} + {{\sin( \frac{a}{2} )}( {{{\cos( \frac{c}{2} )}{\sin( \frac{b}{2} )}( {{{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} + {{\cos( \frac{b}{2} )}{\sin( \frac{c}{2} )}( {{{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\sin( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} - {{\cos( \frac{{d} + {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} + {{\cos( \frac{c}{2} )}{\sin( \frac{a}{2} )}( {{{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{c}{2} )}( {{{\sin( \frac{{d} - {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} + {{\cos( \frac{{d} - {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )} + {{\cos( \frac{a}{2} )}{\cos( \frac{c}{2} )}( {{{\cos( \frac{{d} + {e}}{2} )}{\cos( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} - {1}} )} )}} - {{\sin( \frac{{d} + {e}}{2} )}{\sin( \frac{f}{2} )}{\sin( {\frac{1}{2}( {{g} + {h} + {1}} )} )}}} )}} )}} )^{2}
