( {{{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a - d}{2} )}{\sin( {\frac{1}{2}( {{- c} - f + 3} )} )}} + {{\cos( \frac{c + f}{2} )}{\sin( \frac{3}{2} )}( {{( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( {\frac{1}{2}( {b + e - h} )} )}} + {{\cos( {\frac{1}{2}( {b - e + h} )} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}} - {( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}}} ){\sin( \frac{h}{2} )}}} )}} )} - {{\cos( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}} + {( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}}} ){\sin( \frac{h}{2} )}}} )} - {( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( {\frac{1}{2}( {b + e + h} )} )}} + {{\cos( {\frac{1}{2}( {b - e - h} )} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{g}{2} )}}} )}} )^{2} + ( {{{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( {\frac{1}{2}( {{- c} - f + 3} )} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a - d}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{c + f}{2} )}( {{( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( {\frac{1}{2}( {b + e - h} )} )}} + {{\cos( {\frac{1}{2}( {b - e + h} )} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}} - {( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}}} ){\sin( \frac{h}{2} )}}} )}} )} + {{\sin( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}} + {( {{{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}}} ){\sin( \frac{h}{2} )}}} )} - {( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( {\frac{1}{2}( {b + e + h} )} )}} + {{\cos( {\frac{1}{2}( {b - e - h} )} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{g}{2} )}}} )}} )^{2} + ( {{{\cos( \frac{b - e}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( {\frac{1}{2}( {c + f + 3} )} )}} - {{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{g}{2} )}} - {{\cos( \frac{c + f}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}} - {{\cos( \frac{c + f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{a}{2} )}( {{{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( {\frac{1}{2}( {c + f + 3} )} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{d}{2} )}{\cos( \frac{g}{2} )}( {{{\cos( \frac{b + e}{2} )}{\cos( \frac{h}{2} )}{\sin( {\frac{1}{2}( {c + f + 3} )} )}} + {{\sin( \frac{b + e}{2} )}{\sin( {\frac{1}{2}( {{- c} - f + 3} )} )}{\sin( \frac{h}{2} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{a}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}{\sin( {\frac{1}{2}( {c + f + 3} )} )}} + {{\cos( \frac{c + f}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{g}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}} + {{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{c + f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{h}{2} )}} - {{\cos( \frac{3}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{b - e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{b - e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{d}{2} )}( {{{\cos( \frac{c + f}{2} )}{\sin( \frac{3}{2} )}( {{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} - {{\cos( \frac{a}{2} )}{\sin( \frac{g}{2} )}{\sin( {\frac{1}{2}( {b + e - h} )} )}}} )} + {{\cos( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{g}{2} )}{\sin( {\frac{1}{2}( {b + e + h} )} )}}} )}} )}} )^{2} + ( {{{\cos( \frac{a}{2} )}{\cos( {\frac{1}{2}( {c + f + 3} )} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{c + f}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{g}{2} )}} + {{\cos( \frac{h}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{c + f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{c + f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a - d}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{g}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{3}{2} )}{\cos( \frac{b - e}{2} )}{\cos( \frac{c + f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} - {{\cos( \frac{b - e}{2} )}{\sin( \frac{3}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{c + f}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{h}{2} )}} + {{\cos( \frac{d}{2} )}( {{{\cos( \frac{3}{2} )}{\cos( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} - {{\cos( \frac{a}{2} )}{\sin( \frac{g}{2} )}{\sin( {\frac{1}{2}( {b + e - h} )} )}}} )} - {{\sin( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\sin( \frac{g}{2} )}{\sin( {\frac{1}{2}( {b + e + h} )} )}}} )}} )}} )^{2} + ( {{{\sin( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}( {{{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}} - {{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}}} )} - {( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}} + {{\cos( \frac{b - e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{h}{2} )}}} )} + {{\sin( \frac{g}{2} )}( {{{\cos( \frac{d}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} + {{\cos( \frac{a}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}} - {( {{{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a - d}{2} )}} + {{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}}} ){\sin( \frac{h}{2} )}}} )}} )} + {\frac{1}{4}{\cos( \frac{3}{2} )}{\cos( \frac{c + f}{2} )}( {{\sin( {\frac{1}{2}( {a - b + d + e - g - h} )} )} - {\sin( {\frac{1}{2}( {a + b + d + e - g - h} )} )} - {\sin( {\frac{1}{2}( {a - b - d + e + g - h} )} )} - {\sin( {\frac{1}{2}( {a + b - d + e + g - h} )} )} + {\sin( {\frac{1}{2}( {a - b + d - e - g + h} )} )} - {\sin( {\frac{1}{2}( {a + b + d - e - g + h} )} )} + {\sin( {\frac{1}{2}( {a - b - d - e + g + h} )} )} + {\sin( {\frac{1}{2}( {a + b - d - e + g + h} )} )}} )}} )^{2} + ( {{{\cos( \frac{3}{2} )}{\sin( \frac{c + f}{2} )}( {{{\cos( \frac{g}{2} )}( {{{\cos( \frac{h}{2} )}( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}} - {{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b - e}{2} )}}} )} + {( {{{\cos( \frac{a}{2} )}{\cos( \frac{d}{2} )}{\cos( \frac{b + e}{2} )}} + {{\cos( \frac{b - e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}}} ){\sin( \frac{h}{2} )}}} )} + {{\sin( \frac{g}{2} )}( {{{- {\cos( \frac{d}{2} )}}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{b - e}{2} )}} - {{\cos( \frac{a}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{d}{2} )}{\sin( \frac{b + e}{2} )}} + {( {{{\cos( \frac{b}{2} )}{\cos( \frac{e}{2} )}{\sin( \frac{a - d}{2} )}} + {{\sin( \frac{b}{2} )}{\sin( \frac{a + d}{2} )}{\sin( \frac{e}{2} )}}} ){\sin( \frac{h}{2} )}}} )}} )} + {\frac{1}{4}{\cos( \frac{c + f}{2} )}{\sin( \frac{3}{2} )}( {{\sin( {\frac{1}{2}( {a - b + d + e - g - h} )} )} - {\sin( {\frac{1}{2}( {a + b + d + e - g - h} )} )} - {\sin( {\frac{1}{2}( {a - b - d + e + g - h} )} )} - {\sin( {\frac{1}{2}( {a + b - d + e + g - h} )} )} + {\sin( {\frac{1}{2}( {a - b + d - e - g + h} )} )} - {\sin( {\frac{1}{2}( {a + b + d - e - g + h} )} )} + {\sin( {\frac{1}{2}( {a - b - d - e + g + h} )} )} + {\sin( {\frac{1}{2}( {a + b - d - e + g + h} )} )}} )}} )^{2}
