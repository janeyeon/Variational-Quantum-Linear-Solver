\frac{1}{64}( {{8{❘{{{\sin( \frac{a}{2} )}( {{{\sin( \frac{d}{2} )}( {{{\cos( \frac{e}{2} )}( {{{\sin(2)}{\sin( \frac{f}{2} )}} + {{\cos( \frac{f}{2} )}( {{\sin( \frac{g}{2} )} - {{\cos(2)}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}}} )}} )} + {{\cos(2)}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}}} )} - {{\cos( \frac{d}{2} )}( {{\cos( \frac{g}{2} )} + {{\cos(2)}{\cos( \frac{h}{2} )}{\sin( \frac{g}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\sin(2)}{\sin( \frac{e}{2} )}{\sin( \frac{f}{2} )}} + {{\cos( \frac{f}{2} )}{\sin( \frac{e}{2} )}( {{\sin( \frac{g}{2} )} - {{\cos(2)}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}}} )} - {{\cos(2)}{\cos( \frac{e}{2} )}{\sin( \frac{h}{2} )}}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\cos( \frac{d}{2} )}( {{{\cos( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{{\cos(2)}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}} - {\sin( \frac{g}{2} )}} )} - {{\sin(2)}{\sin( \frac{f}{2} )}}} )} - {{\cos(2)}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}}} )} - {{\sin( \frac{d}{2} )}( {{\cos( \frac{g}{2} )} + {{\cos(2)}{\cos( \frac{h}{2} )}{\sin( \frac{g}{2} )}}} )}} )} - {{\sin( \frac{c}{2} )}( {{{\sin(2)}{\cos( \frac{f}{2} )}} + {{\sin( \frac{f}{2} )}( {{{\cos(2)}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}} - {\sin( \frac{g}{2} )}} )}} )}} )}} )}}❘}^{2}} + {2{❘{{{\sin( \frac{a}{2} )}( {{{\cos( \frac{d}{2} )}( {{\sqrt{2}{\cos( \frac{g}{2} )}} + {{\sin( \frac{g}{2} )}( {{2{\sin( \frac{h}{2} )}} - {\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}}} )}} )} + {{\sin( \frac{d}{2} )}( {{{\cos( \frac{e}{2} )}( {{\sqrt{2}{\sin(2)}{\sin( \frac{f}{2} )}} - {{\cos( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}} - {2{\sin( \frac{h}{2} )}}} )}} )}} )} + {{\sin( \frac{e}{2} )}( {{2{\cos( \frac{h}{2} )}} + {\sqrt{2}{\cos(2)}{\sin( \frac{h}{2} )}}} )}} )}} )} + {{\cos( \frac{a}{2} )}( {{{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\sin( \frac{d}{2} )}( {{\sqrt{2}{\cos( \frac{g}{2} )}} + {{\sin( \frac{g}{2} )}( {{2{\sin( \frac{h}{2} )}} - {\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}}} )}} )} + {{\cos( \frac{d}{2} )}( {{{\cos( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}} - {2{\sin( \frac{h}{2} )}}} )}} )} - {\sqrt{2}{\sin(2)}{\sin( \frac{f}{2} )}}} )} - {{\sin( \frac{e}{2} )}( {{2{\cos( \frac{h}{2} )}} + {\sqrt{2}{\cos(2)}{\sin( \frac{h}{2} )}}} )}} )}} )} - {{\sin( \frac{c}{2} )}( {{\sqrt{2}{\sin(2)}{\cos( \frac{f}{2} )}} + {{\sin( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}} - {2{\sin( \frac{h}{2} )}}} )}} )}} )}} )} - {{\sin( \frac{b}{2} )}( {{{\sin( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}{\cos(2)}{\cos( \frac{h}{2} )}} - {2{\sin( \frac{h}{2} )}}} )}} )} - {\sqrt{2}{\sin(2)}{\sin( \frac{f}{2} )}}} )} + {{\cos( \frac{e}{2} )}( {{2{\cos( \frac{h}{2} )}} + {\sqrt{2}{\cos(2)}{\sin( \frac{h}{2} )}}} )}} )}} )}}❘}^{2}} + {❘{{{- \sqrt{2}}{\cos(2)}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}} - {2\sqrt{2}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\cos( \frac{g}{2} )}{\cos( \frac{h}{2} )}{\sin(2)}{\sin( \frac{a}{2} )}{\sin( \frac{d}{2} )}} - {2{\cos( \frac{h}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{d}{2} )}} - {2\sqrt{2}{\cos(2)}{\cos( \frac{e}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{f}{2} )}{\sin( \frac{d}{2} )}} + {\sqrt{2}{\cos( \frac{e}{2} )}{\sin(2)}{\sin( \frac{a}{2} )}{\sin( \frac{f}{2} )}{\sin( \frac{d}{2} )}} - {\sqrt{2}{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{g}{2} )}{\sin( \frac{d}{2} )}} - {2{\cos( \frac{e}{2} )}{\cos( \frac{f}{2} )}{\cos( \frac{g}{2} )}{\sin( \frac{a}{2} )}{\sin( \frac{h}{2} )}{\sin( \frac{d}{2} )}} + {2\sqrt{2}{\sin(2)}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}{\sin( \frac{d}{2} )}} + {\sqrt{2}{\cos(2)}{\sin( \frac{a}{2} )}{\sin( \frac{e}{2} )}{\sin( \frac{h}{2} )}{\sin( \frac{d}{2} )}} + {{\cos( \frac{d}{2} )}{\sin( \frac{a}{2} )}( {{\sqrt{2}{\cos( \frac{g}{2} )}} - {{\sin( \frac{g}{2} )}( {{\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {2{\sin( \frac{h}{2} )}}} )}} )} + {{\cos( \frac{a}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}( {{2{\cos( \frac{h}{2} )}} - {\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\sin( \frac{h}{2} )}}} )} - {{\sin( \frac{e}{2} )}( {{\sqrt{2}( {{2{\cos(2)}} - {\sin(2)}} ){\sin( \frac{f}{2} )}} + {{\cos( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {2{\sin( \frac{h}{2} )}}} )}} )}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\sin( \frac{c}{2} )}( {{\sqrt{2}{\cos( \frac{f}{2} )}( {{2{\cos(2)}} - {\sin(2)}} )} - {{\sin( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {2{\sin( \frac{h}{2} )}}} )}} )}} )} + {{\cos( \frac{c}{2} )}( {{{\sin( \frac{d}{2} )}( {{\sqrt{2}{\cos( \frac{g}{2} )}} - {{\sin( \frac{g}{2} )}( {{\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {2{\sin( \frac{h}{2} )}}} )}} )} + {{\cos( \frac{d}{2} )}( {{{\sin( \frac{e}{2} )}( {{2{\cos( \frac{h}{2} )}} - {\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\sin( \frac{h}{2} )}}} )} + {{\cos( \frac{e}{2} )}( {{\sqrt{2}( {{2{\cos(2)}} - {\sin(2)}} ){\sin( \frac{f}{2} )}} + {{\cos( \frac{f}{2} )}( {{\sqrt{2}{\sin( \frac{g}{2} )}} + {{\cos( \frac{g}{2} )}( {{\sqrt{2}( {{\cos(2)} + {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {2{\sin( \frac{h}{2} )}}} )}} )}} )}} )}} )}} )}} )} + 4}❘}^{2} + {2{❘{{{\sin( \frac{a}{2} )}( {{{\cos( \frac{d}{2} )}( {{\cos( \frac{g}{2} )} - {{\sin( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )} - {{\sin( \frac{d}{2} )}( {{{\sin( \frac{e}{2} )}( {{\sqrt{2}{\cos( \frac{h}{2} )}} - {( {{\cos(2)} - {2{\sin(2)}}} ){\sin( \frac{h}{2} )}}} )} + {{\cos( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{\sin( \frac{g}{2} )} + {{\cos( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )} - {( {{2{\cos(2)}} + {\sin(2)}} ){\sin( \frac{f}{2} )}}} )}} )}} )} + {{\cos( \frac{a}{2} )}( {{{\sin( \frac{b}{2} )}( {{{\cos( \frac{e}{2} )}( {{\sqrt{2}{\cos( \frac{h}{2} )}} - {( {{\cos(2)} - {2{\sin(2)}}} ){\sin( \frac{h}{2} )}}} )} - {{\sin( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{\sin( \frac{g}{2} )} + {{\cos( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )} - {( {{2{\cos(2)}} + {\sin(2)}} ){\sin( \frac{f}{2} )}}} )}} )} + {{\cos( \frac{b}{2} )}( {{{\cos( \frac{c}{2} )}( {{{\sin( \frac{d}{2} )}( {{\cos( \frac{g}{2} )} - {{\sin( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )} + {{\cos( \frac{d}{2} )}( {{{\sin( \frac{e}{2} )}( {{\sqrt{2}{\cos( \frac{h}{2} )}} - {( {{\cos(2)} - {2{\sin(2)}}} ){\sin( \frac{h}{2} )}}} )} + {{\cos( \frac{e}{2} )}( {{{\cos( \frac{f}{2} )}( {{\sin( \frac{g}{2} )} + {{\cos( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )} - {( {{2{\cos(2)}} + {\sin(2)}} ){\sin( \frac{f}{2} )}}} )}} )}} )} - {{\sin( \frac{c}{2} )}( {{( {{2{\cos(2)}} + {\sin(2)}} ){\cos( \frac{f}{2} )}} + {{\sin( \frac{f}{2} )}( {{\sin( \frac{g}{2} )} + {{\cos( \frac{g}{2} )}( {{( {{\cos(2)} - {2{\sin(2)}}} ){\cos( \frac{h}{2} )}} + {\sqrt{2}{\sin( \frac{h}{2} )}}} )}} )}} )}} )}} )}}❘}^{2}}} )
